create integer variable resultado;
create integer variable result;

function integer somar(integer x, integer y) do
    return x + y;
end

function integer multi(integer x, integer y) do
    return x * y;
end


set resultado to multi(20, 20);
set result to multi(40, 100);

write "%d - %d" resultado result;