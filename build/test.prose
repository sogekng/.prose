create string variable leia;
create integer variable resultado;

function integer somar(integer x, integer y) do
    return x + y;
end

function string ler(string leia) do
    return leia;
end


set leia to ler("Ola mundo");
set resultado to somar(10, 20);

write "%s, o resultado e: %d" leia resultado;